diff --git a/source/Makefile b/source/Makefile
index 602ddf0..7a8e3f5 100644
--- a/source/Makefile
+++ b/source/Makefile
@@ -51,21 +51,25 @@ ifeq ($(MAKECMDGOALS), modules)
 	\t\t-f $(NVIDIA_CONFTEST)/nvidia/Makefile
 endif
 
+# hwpm is now built as part of nvidia-oot to avoid module collision
 hwpm: conftest
 	@if [ ! -d "$(MAKEFILE_DIR)/hwpm" ] ; then \
 		echo "Directory hwpm is not found, exiting.."; \
-		false; \
+		exit 0; \
+	fi
+	@if [ -d "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/hwpm" ] ; then \
+		echo "hwpm is already integrated into nvidia-oot, skipping separate build"; \
+		exit 0; \
 	fi
-	@echo   "================================================================================"
-	@echo   "make $(MAKECMDGOALS) - hwpm ..."
-	@echo   "================================================================================"
-	$(MAKE) -j $(NPROC) ARCH=arm64 \
-		-C $(KERNEL_OUTPUT) \
-		M=$(MAKEFILE_DIR)/hwpm/drivers/tegra/hwpm \
-		CONFIG_TEGRA_OOT_MODULE=m \
-		srctree.hwpm=$(MAKEFILE_DIR)/hwpm \
-		srctree.nvconftest=$(NVIDIA_CONFTEST) \
-		$(MAKECMDGOALS)
+	@echo "Integrating hwpm into nvidia-oot to avoid module collision"
+	@mkdir -p "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/"
+	@if [ ! -e "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/hwpm" ] ; then \
+		ln -s "$(MAKEFILE_DIR)/hwpm/drivers/tegra/hwpm" "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/hwpm"; \
+	fi
+	@if ! grep -q "hwpm" "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/Makefile" 2>/dev/null ; then \
+		echo "obj-m += hwpm/" >> "$(MAKEFILE_DIR)/nvidia-oot/drivers/platform/tegra/Makefile"; \
+	fi
+	@touch "$(MAKEFILE_DIR)/hwpm.integrated"
 
 nvidia-oot: conftest hwpm
 	@if [ ! -d "$(MAKEFILE_DIR)/nvidia-oot" ] ; then \